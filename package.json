{
  "name": "rimworld-workshop-downloader",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "tauri": "cd backend && npx --prefix ../frontend tauri",
    "tauri:icon": "cd backend && npx --prefix ../frontend tauri icon ../assets/icon.png"
  }
}
